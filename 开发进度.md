# 开发进度
## 2026-04-22 登录协议与隐私授权补充
- 更新 `project.config.json` 中的小程序 `appid` 为 `wxd10b7bd6dec89ccb`，并同步开启 `app.json` 的 `__usePrivacyCheck__` 配置。
- 新增 `pages/agreement-user` 与 `pages/agreement-privacy` 页面，补齐登录前可点击查看的《用户协议》《隐私政策》内容页。
- 调整 `pages/loading` 登录页文案与交互，点击“微信一键登录”前先展示协议确认弹窗，并提供协议跳转入口。
- 补充 `wx.requirePrivacyAuthorize` 调用逻辑，在用户同意协议后继续拉起微信官方隐私授权流程；隐私政策链接优先尝试 `wx.openPrivacyContract`，失败时回退到本地隐私页。
- 对登录页相关脚本完成静态校验，确认 `pages/loading/index.js` 语法通过，协议确认链路可继续联调。
## 2026-04-22 首页三餐换餐去重
- 调整 `utils/store.js` 本地换餐逻辑，换早餐/午餐/晚餐时会同时避开当前三餐中已占用的食谱，优先保证当日三餐不重复。
- 调整 `utils/api.js` 真实后端请求参数，换餐时携带当前首页三餐的 `exclude_recipe_ids`，避免后端返回与另外两餐重复的结果。
- 调整 `backend/routes/recommendation.js` 与 `backend/services/planner.js`，后端换餐服务支持排除指定食谱列表；当可选食谱充足时优先返回不重复结果，不足时再回退兜底。
- 对前后端相关脚本完成静态校验，确认首页“三餐换一换”去重逻辑语法通过，可继续在微信开发者工具联调验证。
## 2026-04-22 数据源切换配置补充
- 调整 `utils/api.js`，新增 `mock_api_enabled` 配置项，默认值为 `false`。
- 仅当 `mock_api_enabled === true` 时，接口层才会走本地 `store` mock 数据；否则统一走真实后端接口。
- 移除原先接口失败后自动回退本地 mock 的静默降级逻辑，后端请求失败时会直接暴露错误，便于在微信开发者工具中定位联调问题。
- 补充 `isMockEnabled()` 与 `setMockEnabled()` 导出，方便在开发者工具控制台或业务代码中显式切换数据源模式。
## 2026-04-22 个人资料保存联调补充
- 新增 `PUT /api/auth/profile` 后端接口，支持更新当前登录用户的 `nickname` 与 `avatar_url`。
- 调整 `utils/api.js` 的 `saveUserProfile`，非 mock 模式下改为真实调用后端保存资料，再同步回本地状态，避免刷新后被旧昵称覆盖。
- 保留 `bio` 在前端本地状态中的同步展示逻辑，确保当前版本保存昵称与头像后页面即时更新。
- 对 `backend/routes/auth.js` 与 `utils/api.js` 完成静态校验，确认个人资料保存链路语法通过。
## 2026-04-22 个人说明与宝宝头像补充
- 更新 `supabase/schema.sql`，为 `users` 增加 `bio` 字段、为 `babies` 增加 `avatar_url` 字段；新增迁移文件 `supabase/migrations/20260422_add_user_bio_and_baby_avatar.sql` 供已有库执行。
- 调整 `backend/routes/auth.js`，`PUT /api/auth/profile` 现支持同步保存 `bio`。
- 调整 `backend/routes/baby.js`，宝宝创建/编辑接口现支持保存 `avatar_url`。
- 调整 `pages/baby-edit`，新增宝宝头像上传入口，并在切换性别时保留已上传的自定义头像。
- 调整 `pages/mine` 的“我的宝宝”信息栏，新增宝宝头像展示；首页顶栏继续使用 `currentBaby.avatarImage`，在数据链路打通后自动展示上传头像。
- 调整 `utils/api.js` 的宝宝数据映射与保存逻辑，确保宝宝头像和个人说明在真实接口模式下可读写并回显。

## 2026-04-21

### 需求拆解
- 已通读《需求文档 - 育儿助手小程序 UE设计.md》，整理出 4 个 Tab 页面、3 个业务子页面、宝宝档案编辑入口与 4 条核心流程。
- 已确认当前仓库仅有 Supabase/服务层代码，缺少原生微信小程序工程，需要从应用骨架开始补建。

### 工程初始化
- 新建原生小程序入口文件：`app.js`、`app.json`、`app.wxss`、`project.config.json`、`sitemap.json`。
- 建立基础目录结构：`pages/`、`components/`、`utils/`。
- 配置 4 个底部 Tab：`首页`、`食谱库`、`生长记录`、`我的`。

### 数据与状态层
- 新增 `utils/mock-data.js`，补充用户、宝宝、食谱、生长记录、首页推荐等 MVP 示例数据。
- 新增 `utils/store.js`，封装本地状态初始化、宝宝切换、首页推荐换餐、收藏、做过记录、生长记录新增、周计划生成、备菜清单聚合等逻辑。
- 新增 `utils/api.js` 与 `utils/auth.js`，统一页面侧调用方式，为后续接真实接口预留替换点。

### 公共组件
- 新增 `components/section-card`，统一页面区块容器样式。
- 新增 `components/recipe-card`，用于首页推荐、食谱列表和推荐卡片复用。
- 新增 `components/growth-chart`，使用轻量柱形趋势图表达生长曲线趋势。

### 页面开发
- 完成 `pages/loading`：启动页与微信一键登录入口。
- 完成 `pages/home`：宝宝切换、今日三餐、换一换、周计划入口、快捷入口、猜你喜欢。
- 完成 `pages/recipes`：搜索、月龄筛选、标签筛选、列表展示。
- 完成 `pages/recipe-detail`：主图、标签、食材、步骤、小贴士、营养分析、收藏与做过操作。
- 完成 `pages/growth`：最新记录、记录新数据弹层、智能解读、推荐食谱、历史记录。
- 完成 `pages/mine`：用户信息、宝宝列表、内容统计、长辈模式、设置入口。
- 完成 `pages/weekly-plan`：本周三餐计划、一键换新、备菜清单。
- 完成 `pages/baby-edit`：创建/编辑宝宝档案、过敏源与偏好选择。

### 当前状态
- 已形成可在微信开发者工具中打开的原生小程序 MVP。
- 当前使用本地模拟数据驱动，尚未与仓库内 Supabase/Express 后端完成真实联调。

### 校验记录
- 已完成新增小程序 JS 文件静态语法检查（`node --check` 通过）。
- 已排查并移除 WXML 中高风险的方法调用表达式，降低微信开发者工具编译报错概率。

### 第二轮联调开发
- 新增后端公共能力：`backend/lib/supabase.js`、`backend/lib/config.js`、`backend/lib/load-env.js`、`backend/middleware/auth.js`。
- 补齐后端业务路由并挂载到 Express：`/api/baby`、`/api/growth-record`、`/api/recipe`，同时为认证补充 `/api/auth/dev-login`。
- 修正后端认证服务与当前 `supabase/schema.sql` 的字段对齐问题，移除对不存在列 `profile`、`last_login_at`、`last_sign_out_at` 的硬依赖。
- 重写 `utils/api.js`，增加 `wx.request` 真实请求封装、Bearer Token 注入、开发登录兜底、远端数据到本地状态的同步逻辑。
- 已将以下能力优先切到真实后端：登录、宝宝列表/创建/更新、宝宝切换、生长记录查询/新增、食谱列表、食谱详情。
- 对于尚未完成后端支持的功能（如收藏、做过、首页推荐、周计划），前端仍保留本地 mock 兜底，确保当前小程序可继续演示。

### 第二轮校验
- 已完成后端新增路由与前端请求层的静态语法检查（`node --check` 通过）。
- 已补充首次登录无宝宝档案的边界处理，自动引导进入宝宝档案创建页。

### 第三轮联调开发
- 新增数据库迁移 `supabase/migrations/20260421_add_made_recipes.sql`，补充“做过食谱”持久化表。
- 补齐后端“剩余核心流程”：
  - `POST /api/recipe/:recipeId/collect`
  - `POST /api/recipe/:recipeId/mark-made`
  - `GET /api/recipe/collections/list`
  - `GET /api/recommendation/today`
  - `POST /api/recommendation/swap-meal`
  - `POST /api/weekly-plan/generate`
  - `POST /api/weekly-plan/shopping-list`
- 新增 `backend/services/planner.js`，将今日推荐、换餐、周计划、备菜清单统一收敛为服务端计算逻辑。
- 前端 `utils/api.js` 已继续切换到真实后端：收藏、做过、今日推荐、换一换、周计划、备菜清单优先走远端，失败时回退本地 mock。
- 由于当前本机 Node 运行时低于 `@supabase/supabase-js` 推荐版本，新增 `backend/lib/fetch-polyfill.js` 兼容层，保证本地开发环境可启动服务。

### 第三轮校验
- 已再次通过新增/修改文件的 `node --check` 静态校验。
- 已实际启动本地后端服务，确认 Express 可在 `http://localhost:3000` 正常监听。

### 数据补充
- 新增 `supabase/seed.sql`，整理 8 道核心食谱为可重复执行的种子数据。
- 为 `recipes.name` 补充唯一索引，支持 `seed.sql` 使用 `on conflict (name)` 幂等导入。

### 文档补充
- 新增 `supabase/执行顺序与联调清单.md`，整理 SQL 执行顺序、后端启动方式、小程序联调步骤与常见问题。
- 更新根目录 `README.md` 与 `backend/README.md`，补充种子数据与联调前置说明。

### 验证补充
- 新增 `backend/scripts/smoke-test.js` 与 `npm run smoke`，用于自动验证登录、宝宝、食谱、收藏、做过、生长、推荐、周计划主链路。
- 将 `backend/app.js` 改为可导出 `startServer()`，便于在 smoke test 中同进程拉起并关闭服务。
- 已实际运行 smoke test：本地服务可正常启动，当前阻塞为远端 `SUPABASE_URL` 返回 Cloudflare `521 Web server is down`，说明上游 Supabase 项目暂不可达。

### 下一步建议
- 对接真实 API：优先替换登录、宝宝 CRUD、食谱列表/详情、收藏、生长记录接口。
- 引入真实图表方案：若要更接近设计稿，可接入 `echarts-for-weixin` 渲染曲线与分位线。
- 补充真机调试与交互细节：页面转场、骨架屏、空状态、错误态与长辈模式视觉简化。
## 2026-04-21 验收反馈第二轮修正
- 调整 `pages/weekly-plan` 底部“一键生成备菜清单”按钮的文字居中样式。
- 调整 `pages/growth` 记录弹层，日期改为日历选择器，保存按钮补齐居中样式。
- 移除 `pages/home` 与 `pages/mine` 的“我的内容”板块，精简首页和我的页结构。
- 调整 `pages/recipe-detail` 底部“收藏食谱”“我做过了”按钮的文字居中样式。
- 调整 `pages/baby-edit` 中新增过敏原、饮食偏好的“确定”按钮居中样式。
## 2026-04-22 验收反馈第三轮修正
- 调整 `pages/loading` 登录按钮，补齐文字居中样式。
- 调整 `pages/home` 三餐卡片中的“换一换”按钮，对比度更强，避免与卡片底色混在一起。
- 重写 `components/growth-chart`，将生长图从柱状表达改为折线/曲线趋势表达，更贴近生长曲线阅读习惯。
- 调整 `pages/growth` “智能解读”模块上下间距与信息分组，避免内容粘连。
- 新增 `assets/tabbar/*.png` 并更新 `app.json`，为底部 Tab 补充图标与选中态视觉区分。
## 2026-04-22 WHO 生长曲线接入
- 下载 WHO 官方 `0-5 岁 Length/Height-for-age` 男女 percentiles expanded tables，并落库到 `assets/who/` 作为原始数据来源。
- 生成项目内可直接使用的 `utils/who-lhfa-data.json`，保留 `day / L / M / S / P3 / P50 / P97` 字段，支持本地插值计算。
- 新增 `utils/growth-standard.js`，实现年龄天数换算、WHO 标准插值、LMS z-score 计算、percentile 估算，以及曲线绘图数据构建。
- 重构 `components/growth-chart`，改为按 WHO `3% / 50% / 97%` 参考线和宝宝实测点绘制趋势图，并增加“最近”垂直标记。
- 调整 `pages/growth` 与 `utils/store.js`，实现 24 月以下显示“身长”、24 月及以上显示“身高”，并将智能解读改为基于 WHO 分位结果生成。
# 开发进度

## 2026-04-23 头像持久化与回显修正
- 修正 `utils/avatar.js` 的头像地址判断，不再把 `http://127.0.0.1:18729/__tmp__` 等微信开发者工具临时地址当成可持久化头像地址。
- 补充头像保存前转换逻辑：个人资料头像、宝宝档案头像会在保存前转为 `data:image/...` 形式再提交后端，转换失败时提示用户重新选择，避免继续写入失效临时地址。
- 修正用户资料回显逻辑：`syncRemoteUser` 现在会读取后端返回的 `avatar_url` 并同步到 `avatarImage`，清缓存重新登录后也能显示数据库中的头像。
- 修正宝宝头像回显兜底：如果数据库里已有旧的临时头像地址，前端会回退默认宝宝头像，重新选择并保存后可覆盖为持久化头像。
- 已完成 `utils/avatar.js`、`utils/api.js`、`utils/store.js`、`pages/mine/index.js`、`pages/baby-edit/index.js`、`backend/routes/auth.js`、`backend/routes/baby.js` 的静态语法检查。
# 开发进度

## 2026-04-23 生长记录推荐食谱修复
- 修正 `utils/store.js` 中智能解读推荐食谱的生成方式，不再固定读取 `recipe-4`、`recipe-5`，改为按当前宝宝月龄、过敏源筛选可用食谱，并优先推荐含钙、铁、蛋白等成长相关关键词的食谱。
- 调整 `pages/growth/index.js`，进入生长记录页时先尝试刷新当前宝宝可用食谱，再读取生长数据生成推荐，避免真实后端食谱 ID 与本地 mock ID 不一致导致空白卡片。
- 调整 `pages/growth/index.wxml`，仅当存在有效推荐食谱时展示“推荐食谱”区域，避免出现空白推荐卡片。
- 已完成 `utils/store.js` 与 `pages/growth/index.js` 静态语法检查。
# 开发进度

## 2026-04-23 生长记录体重曲线补充
- 下载 WHO 官方 `Weight-for-age` 0-5 岁男女百分位 expanded tables，并生成 `utils/who-wfa-data.js`，用于体重曲线 3% / 50% / 97% 参考线与体重分位计算。
- 扩展 `utils/growth-standard.js`，在原身长/身高曲线基础上新增体重曲线构建、体重分位估算，继续使用 WHO LMS 参数和百分位表插值。
- 调整 `utils/store.js`，生长记录数据现在返回 `chartList`，包含身长/身高曲线和体重曲线；智能解读增加体重分位、体重趋势和通俗说明。
- 重构 `components/growth-chart` 为横向 `swiper`，默认展示身长/身高曲线，右滑查看体重曲线，并增加滑动提示与分页圆点。
- 调整 `pages/growth`，接入双曲线数据，并让智能解读支持分段展示身高/身长与体重说明。
- 已完成 `utils/growth-standard.js`、`utils/store.js`、`pages/growth/index.js`、`components/growth-chart/index.js`、`utils/who-wfa-data.js` 静态语法检查。
# 开发进度

## 2026-04-23 添加食谱入口预览
- 在 `pages/recipes` 食谱库顶部新增“添加食谱”入口卡片，仅在全部食谱模式下展示，点击进入新增食谱页。
- 新增 `pages/recipe-add` 页面，用于预览用户自定义食谱记录表单，包含封面入口、食谱名称、适合月龄、制作时间、主要食材、制作步骤和公开分享开关。
- 更新 `app.json` 注册新增页面路径，确保可从食谱库正常跳转。
- 当前版本仅用于入口和页面效果预览，保存、图片上传、审核与分享链路后续再接后端。
- 已完成 `pages/recipes/index.js`、`pages/recipe-add/index.js` 静态语法检查，以及 `app.json`、`pages/recipe-add/index.json` JSON 校验。
# 开发进度

## 2026-04-23 用户自建食谱链路
- 新增 `supabase/migrations/20260423_add_user_recipes.sql`，为 `recipes` 补充 `created_by_user_id`、`image_text`、`is_public`、`audit_status` 字段，用于区分系统食谱、用户自建食谱、公开/私密状态。
- 扩展 `backend/routes/recipe.js`，新增 `POST /api/recipe/create` 保存用户食谱、`GET /api/recipe/my/list` 查询我的食谱；食谱库列表和详情增加公开/本人可见过滤，避免私密食谱被其他用户看到。
- 调整 `pages/recipe-add`，从静态预览改为真实表单保存，支持名称、月龄、制作时间、食材、步骤、小贴士、营养说明和公开分享开关；保存成功后进入食谱详情页。
- 调整 `utils/api.js` 与 `utils/store.js`，新增 `createRecipe`、`getCreatedRecipes` 链路，真实接口和 mock 模式均可保存/查看我的食谱。
- 调整 `pages/home` 的“我的内容”，新增“我的食谱”入口；点击后进入食谱库的“我的食谱”列表模式。
- 调整 `pages/recipes`，新增 `created` 列表模式和空状态提示；调整 `components/recipe-card` 与 `pages/recipe-detail`，我的食谱显示“公开/私密”标识，卡片点击继续复用食谱详情。
- 已完成前端相关 JS、`utils/api.js`、`utils/store.js`、`backend/routes/recipe.js` 静态语法检查，以及 `app.json`、`pages/recipe-add/index.json` JSON 校验。
# 开发进度

## 2026-04-23 自制食谱图标与公开状态切换
- 修复自制食谱字段迁移未执行时的后端报错提示，遇到 `recipes.is_public` 等字段缺失会明确提示先执行 `20260423_add_user_recipes.sql`。
- 扩展 `pages/recipe-add`，新增食谱图标选择器：根据食谱名称关键词自动推荐图标，也支持用户从候选图标中手动选择或输入自定义图标。
- 调整自制食谱保存链路，保存时使用用户选择的 `imageText`，食谱卡片和详情页同步展示该图标。
- 扩展 `components/recipe-card`，我的食谱卡片右下角公开/私密标识支持点击，不再触发进入详情。
- 调整 `pages/recipes`，点击公开/私密标识会弹窗确认，确认后调用 `PATCH /api/recipe/:recipeId/visibility` 切换公开状态并刷新列表。
- 扩展 `backend/routes/recipe.js`、`utils/api.js`、`utils/store.js`，补齐真实后端与 mock 模式下的公开/私密切换能力。
- 已完成 `backend/routes/recipe.js`、`components/recipe-card/index.js`、`pages/recipes/index.js`、`pages/recipe-add/index.js`、`utils/api.js`、`utils/store.js` 静态语法检查。
# 开发进度

## 2026-04-23 自制食谱编辑与标签完善
- 调整 `pages/recipe-add` 表单顺序，改为先填写食谱名称，再根据名称推荐食谱图标；移除自定义图标输入，仅保留图标选项，页面更简洁。
- 新增自制食谱功效标签选择，支持默认标签 `自制食谱 / 补铁 / 补钙 / 增重 / 补蛋白 / 易消化 / 手指食物` 多选，并支持输入自定义标签。
- 调整自制食谱保存逻辑，使用用户选择的标签提交，不再固定只有“自制食谱”一个标签。
- 在自制食谱卡片上新增“编辑”按钮，点击进入食谱编辑模式；编辑页会回填名称、图标、月龄、制作时间、标签、食材、步骤、小贴士和营养说明。
- 扩展 `backend/routes/recipe.js`、`utils/api.js`、`utils/store.js`，新增自制食谱更新能力，编辑保存后会更新数据库/本地状态并返回详情页。
- 已完成 `pages/recipe-add/index.js`、`pages/recipes/index.js`、`components/recipe-card/index.js`、`utils/api.js`、`utils/store.js`、`backend/routes/recipe.js` 静态语法检查。
# 开发进度

## 2026-04-23 自制食谱卡片操作样式调整
- 调整 `components/recipe-card` 中自制食谱卡片的操作区，将“编辑”和“公开/私密”改为底部右侧一行并排展示。
- “编辑”按钮增加铅笔图标，“公开”增加开锁图标，“私密”增加闭锁图标，和上方食谱标签区形成区分。
- 使用 `.is-mine` 类增加卡片底部空间，避免依赖小程序兼容性不稳定的高级 CSS 选择器。
- 已完成 `components/recipe-card/index.js` 静态语法检查。
# 开发进度

## 2026-04-23 食谱卡片编辑图标一致性
- 调整 `components/recipe-card` 的自制食谱“编辑”按钮图标，移除文字铅笔符号，改为与【我的】顶栏信息一致的 CSS 铅笔图形。
- 复用同样的 `rotate(-32deg)` 方向、笔尖和笔尾视觉细节，保持页面图标风格一致。
- 已完成 `components/recipe-card/index.js` 静态语法检查。
# 开发进度

## 2026-04-23 周计划生成前必选食谱
- 调整【首页】“一键生成周计划”入口，点击“立即生成”后先弹出食谱选择层，不再直接进入周计划页。
- 选择层按“我的收藏 / 做过食谱 / 我的食谱”三组展示食谱名称，点击名称可勾选或取消勾选。
- 新增“确认生成”操作；勾选后会把食谱 ID 作为 `required_recipe_ids` 传入周计划生成接口，未勾选时沿用原生成逻辑。
- 扩展 `backend/routes/weekly-plan.js` 与 `backend/services/planner.js`，周计划生成支持必选食谱，并确保勾选食谱至少出现在本周计划表中。
- 扩展 `utils/api.js` 与 `utils/store.js`，真实接口和 mock 模式均支持必选食谱生成；周计划页进入时会优先展示首页刚生成的计划，避免重新生成覆盖必选结果。
- 调整备菜清单生成逻辑，若本地已有当前周计划，则优先基于当前计划生成清单，避免和用户刚勾选生成的计划不一致。
- 已完成 `pages/home/index.js`、`pages/weekly-plan/index.js`、`utils/api.js`、`utils/store.js`、`backend/routes/weekly-plan.js`、`backend/services/planner.js` 静态语法检查。

## 2026-04-23 周计划去重与确认按钮优化
- 调整【首页】生成周计划前的食谱选择弹窗，仅保留“确认生成”一个操作；未勾选任何食谱时沿用原生成逻辑，勾选后优先把选中食谱加入本周计划。
- 补充按钮居中样式，避免小程序按钮默认样式导致文案偏移。
- 重构本地 mock 与后端周计划排餐算法：菜单数量足够时尽量 21 个餐位不重复；菜单不足时优先避免同日重复，并尽量让同一食谱间隔 3 天后再出现。
- 已完成 `pages/home/index.js`、`utils/store.js`、`utils/api.js`、`backend/services/planner.js` 静态语法检查。

## 2026-04-23 本周菜单确认与单日换新
- 在【周计划】周一到周日卡片右上角新增“换一换”按钮，支持单独更换当天三餐；更换后本周菜单回到未确认状态，避免和备菜清单不一致。
- 将“生成备菜清单”定义为确认本周菜单动作，点击后会把当前周计划保存为本周已确定菜单。
- 首页【今日三餐】现在会优先读取已确定的本周菜单中“今天”的三餐，避免确认周计划后首页仍显示随机推荐。
- 首页【一键生成周计划】在已有已确定菜单时显示“查看本周菜单”按钮，同时保留“重新生成”入口。
- 已完成 `utils/store.js`、`utils/api.js`、`pages/weekly-plan/index.js`、`pages/home/index.js` 静态语法检查。

## 2026-04-23 周计划后端持久化
- 调整首页【一键生成周计划】按钮栅格样式，改为等宽两列 `minmax(0, 1fr)`，并收紧字号与内边距，解决“查看本周菜单 / 重新生成”撑出容器的问题。
- 新增 `weekly_plan_snapshots` 表迁移：按 `用户 + 宝宝 + 周一日期` 保存本周菜单快照、确认状态和更新时间。
- 扩展 `backend/routes/weekly-plan.js` 与 `backend/services/planner.js`，新增读取当前周计划、保存周计划、生成即持久化、生成备菜清单时确认菜单的后端能力。
- 调整 `utils/api.js`：首页和周计划页会优先读取后端已保存的本周菜单；单日“换一换”后也会回写后端，清缓存后仍可恢复本周菜单。
- 已完成 `backend/services/planner.js`、`backend/routes/weekly-plan.js`、`utils/api.js`、`utils/store.js`、`pages/home/index.js` 静态语法检查。

## 2026-04-23 家庭共享需求补充
- 在 [需求文档 - 育儿助手小程序 UE设计.md](G:/works/liuli/mommy-kitchen-helper/需求文档%20-%20育儿助手小程序%20UE设计.md) 中新增“我的家庭”页面说明，明确家庭信息、成员管理、邀请码加入和角色设计。
- 补充“家庭共享流程”，定义妈妈创建家庭、邀请爸爸加入后共享宝宝、生长记录、周计划等数据的主流程。
- 补充后端数据库设计：新增 `families`、`family_members`、`family_invites`，并为宝宝、做过记录、生长记录、周计划增加 `family_id` 归属建议。
- 补充家庭共享 API 设计、MVP 范围和现有单用户模型迁移方案，作为后续正式开发的依据。

## 2026-04-23 家庭共享技术方案细化
- 在需求文档中继续补充“家庭共享技术落地方案”，明确数据库迁移顺序、历史数据回填策略、后端改造清单、前端改造清单、权限矩阵、推荐开发顺序和联调验收点。
- 将“家庭共享”从产品想法进一步收敛为可执行开发方案，后续可直接据此拆解 Supabase migration、接口开发和页面改造任务。

## 2026-04-23 家庭共享后端骨架
- 新增 `supabase/migrations/20260423_add_family_sharing.sql`，创建 `families`、`family_members`、`family_invites`，并为 `babies`、`growth_records`、`weekly_plan_snapshots` 增加 `family_id`，同时附带历史数据回填 SQL。
- 扩展 `supabase/schema.sql` 同步家庭共享结构，便于新环境一次性初始化。
- 新增 `backend/services/family.js` 与 `backend/routes/family.js`，补齐当前家庭、成员列表、生成邀请码、邀请码加入、修改角色、移除成员等接口骨架。
- 调整 `backend/middleware/auth.js`、`backend/routes/auth.js`，登录和鉴权后自动为用户兜底创建默认家庭，并在登录/用户资料返回中附带 `family_id`、`family_role`。
- 调整 `backend/routes/baby.js`、`backend/routes/growth.js`、`backend/routes/recommendation.js`、`backend/routes/weekly-plan.js` 与 `backend/services/planner.js`，让宝宝、生长记录、首页推荐、周计划按家庭维度读取与保存，支撑爸爸妈妈共享同一份数据。
- 已完成 `backend/app.js`、`backend/middleware/auth.js`、`backend/routes/family.js`、`backend/routes/baby.js`、`backend/routes/growth.js`、`backend/routes/recommendation.js`、`backend/routes/weekly-plan.js`、`backend/services/family.js`、`backend/services/planner.js` 静态语法检查。

## 2026-04-23 家庭共享前端入口
- 在【我的】页新增“我的家庭”入口卡片，进入后可查看当前家庭、成员列表和共享说明。
- 新增 `pages/family` 页面，接入 `GET /api/family/current`、`POST /api/family/invite`、`POST /api/family/join`、`PATCH /api/family/name`，支持查看家庭信息、生成邀请码、输入邀请码加入、修改家庭名称。
- 扩展 `utils/api.js` 与 `utils/store.js`，补齐家庭信息的真实接口调用与 mock 兜底，便于前后端联调和本地演示。
- 更新 `app.json` 注册家庭页面路由。
- 已完成 `utils/api.js`、`utils/store.js`、`pages/mine/index.js`、`pages/family/index.js` 静态语法检查，以及 `pages/family/index.json`、`app.json` JSON 校验。
