# 开发进度
## 2026-04-22 登录协议与隐私授权补充
- 更新 `project.config.json` 中的小程序 `appid` 为 `wxd10b7bd6dec89ccb`，并同步开启 `app.json` 的 `__usePrivacyCheck__` 配置。
- 新增 `pages/agreement-user` 与 `pages/agreement-privacy` 页面，补齐登录前可点击查看的《用户协议》《隐私政策》内容页。
- 调整 `pages/loading` 登录页文案与交互，点击“微信一键登录”前先展示协议确认弹窗，并提供协议跳转入口。
- 补充 `wx.requirePrivacyAuthorize` 调用逻辑，在用户同意协议后继续拉起微信官方隐私授权流程；隐私政策链接优先尝试 `wx.openPrivacyContract`，失败时回退到本地隐私页。
- 对登录页相关脚本完成静态校验，确认 `pages/loading/index.js` 语法通过，协议确认链路可继续联调。
## 2026-04-22 首页三餐换餐去重
- 调整 `utils/store.js` 本地换餐逻辑，换早餐/午餐/晚餐时会同时避开当前三餐中已占用的食谱，优先保证当日三餐不重复。
- 调整 `utils/api.js` 真实后端请求参数，换餐时携带当前首页三餐的 `exclude_recipe_ids`，避免后端返回与另外两餐重复的结果。
- 调整 `backend/routes/recommendation.js` 与 `backend/services/planner.js`，后端换餐服务支持排除指定食谱列表；当可选食谱充足时优先返回不重复结果，不足时再回退兜底。
- 对前后端相关脚本完成静态校验，确认首页“三餐换一换”去重逻辑语法通过，可继续在微信开发者工具联调验证。

## 2026-04-21

### 需求拆解
- 已通读《需求文档 - 育儿助手小程序 UE设计.md》，整理出 4 个 Tab 页面、3 个业务子页面、宝宝档案编辑入口与 4 条核心流程。
- 已确认当前仓库仅有 Supabase/服务层代码，缺少原生微信小程序工程，需要从应用骨架开始补建。

### 工程初始化
- 新建原生小程序入口文件：`app.js`、`app.json`、`app.wxss`、`project.config.json`、`sitemap.json`。
- 建立基础目录结构：`pages/`、`components/`、`utils/`。
- 配置 4 个底部 Tab：`首页`、`食谱库`、`生长记录`、`我的`。

### 数据与状态层
- 新增 `utils/mock-data.js`，补充用户、宝宝、食谱、生长记录、首页推荐等 MVP 示例数据。
- 新增 `utils/store.js`，封装本地状态初始化、宝宝切换、首页推荐换餐、收藏、做过记录、生长记录新增、周计划生成、备菜清单聚合等逻辑。
- 新增 `utils/api.js` 与 `utils/auth.js`，统一页面侧调用方式，为后续接真实接口预留替换点。

### 公共组件
- 新增 `components/section-card`，统一页面区块容器样式。
- 新增 `components/recipe-card`，用于首页推荐、食谱列表和推荐卡片复用。
- 新增 `components/growth-chart`，使用轻量柱形趋势图表达生长曲线趋势。

### 页面开发
- 完成 `pages/loading`：启动页与微信一键登录入口。
- 完成 `pages/home`：宝宝切换、今日三餐、换一换、周计划入口、快捷入口、猜你喜欢。
- 完成 `pages/recipes`：搜索、月龄筛选、标签筛选、列表展示。
- 完成 `pages/recipe-detail`：主图、标签、食材、步骤、小贴士、营养分析、收藏与做过操作。
- 完成 `pages/growth`：最新记录、记录新数据弹层、智能解读、推荐食谱、历史记录。
- 完成 `pages/mine`：用户信息、宝宝列表、内容统计、长辈模式、设置入口。
- 完成 `pages/weekly-plan`：本周三餐计划、一键换新、备菜清单。
- 完成 `pages/baby-edit`：创建/编辑宝宝档案、过敏源与偏好选择。

### 当前状态
- 已形成可在微信开发者工具中打开的原生小程序 MVP。
- 当前使用本地模拟数据驱动，尚未与仓库内 Supabase/Express 后端完成真实联调。

### 校验记录
- 已完成新增小程序 JS 文件静态语法检查（`node --check` 通过）。
- 已排查并移除 WXML 中高风险的方法调用表达式，降低微信开发者工具编译报错概率。

### 第二轮联调开发
- 新增后端公共能力：`backend/lib/supabase.js`、`backend/lib/config.js`、`backend/lib/load-env.js`、`backend/middleware/auth.js`。
- 补齐后端业务路由并挂载到 Express：`/api/baby`、`/api/growth-record`、`/api/recipe`，同时为认证补充 `/api/auth/dev-login`。
- 修正后端认证服务与当前 `supabase/schema.sql` 的字段对齐问题，移除对不存在列 `profile`、`last_login_at`、`last_sign_out_at` 的硬依赖。
- 重写 `utils/api.js`，增加 `wx.request` 真实请求封装、Bearer Token 注入、开发登录兜底、远端数据到本地状态的同步逻辑。
- 已将以下能力优先切到真实后端：登录、宝宝列表/创建/更新、宝宝切换、生长记录查询/新增、食谱列表、食谱详情。
- 对于尚未完成后端支持的功能（如收藏、做过、首页推荐、周计划），前端仍保留本地 mock 兜底，确保当前小程序可继续演示。

### 第二轮校验
- 已完成后端新增路由与前端请求层的静态语法检查（`node --check` 通过）。
- 已补充首次登录无宝宝档案的边界处理，自动引导进入宝宝档案创建页。

### 第三轮联调开发
- 新增数据库迁移 `supabase/migrations/20260421_add_made_recipes.sql`，补充“做过食谱”持久化表。
- 补齐后端“剩余核心流程”：
  - `POST /api/recipe/:recipeId/collect`
  - `POST /api/recipe/:recipeId/mark-made`
  - `GET /api/recipe/collections/list`
  - `GET /api/recommendation/today`
  - `POST /api/recommendation/swap-meal`
  - `POST /api/weekly-plan/generate`
  - `POST /api/weekly-plan/shopping-list`
- 新增 `backend/services/planner.js`，将今日推荐、换餐、周计划、备菜清单统一收敛为服务端计算逻辑。
- 前端 `utils/api.js` 已继续切换到真实后端：收藏、做过、今日推荐、换一换、周计划、备菜清单优先走远端，失败时回退本地 mock。
- 由于当前本机 Node 运行时低于 `@supabase/supabase-js` 推荐版本，新增 `backend/lib/fetch-polyfill.js` 兼容层，保证本地开发环境可启动服务。

### 第三轮校验
- 已再次通过新增/修改文件的 `node --check` 静态校验。
- 已实际启动本地后端服务，确认 Express 可在 `http://localhost:3000` 正常监听。

### 数据补充
- 新增 `supabase/seed.sql`，整理 8 道核心食谱为可重复执行的种子数据。
- 为 `recipes.name` 补充唯一索引，支持 `seed.sql` 使用 `on conflict (name)` 幂等导入。

### 文档补充
- 新增 `supabase/执行顺序与联调清单.md`，整理 SQL 执行顺序、后端启动方式、小程序联调步骤与常见问题。
- 更新根目录 `README.md` 与 `backend/README.md`，补充种子数据与联调前置说明。

### 验证补充
- 新增 `backend/scripts/smoke-test.js` 与 `npm run smoke`，用于自动验证登录、宝宝、食谱、收藏、做过、生长、推荐、周计划主链路。
- 将 `backend/app.js` 改为可导出 `startServer()`，便于在 smoke test 中同进程拉起并关闭服务。
- 已实际运行 smoke test：本地服务可正常启动，当前阻塞为远端 `SUPABASE_URL` 返回 Cloudflare `521 Web server is down`，说明上游 Supabase 项目暂不可达。

### 下一步建议
- 对接真实 API：优先替换登录、宝宝 CRUD、食谱列表/详情、收藏、生长记录接口。
- 引入真实图表方案：若要更接近设计稿，可接入 `echarts-for-weixin` 渲染曲线与分位线。
- 补充真机调试与交互细节：页面转场、骨架屏、空状态、错误态与长辈模式视觉简化。
## 2026-04-21 验收反馈第二轮修正
- 调整 `pages/weekly-plan` 底部“一键生成备菜清单”按钮的文字居中样式。
- 调整 `pages/growth` 记录弹层，日期改为日历选择器，保存按钮补齐居中样式。
- 移除 `pages/home` 与 `pages/mine` 的“我的内容”板块，精简首页和我的页结构。
- 调整 `pages/recipe-detail` 底部“收藏食谱”“我做过了”按钮的文字居中样式。
- 调整 `pages/baby-edit` 中新增过敏原、饮食偏好的“确定”按钮居中样式。
## 2026-04-22 验收反馈第三轮修正
- 调整 `pages/loading` 登录按钮，补齐文字居中样式。
- 调整 `pages/home` 三餐卡片中的“换一换”按钮，对比度更强，避免与卡片底色混在一起。
- 重写 `components/growth-chart`，将生长图从柱状表达改为折线/曲线趋势表达，更贴近生长曲线阅读习惯。
- 调整 `pages/growth` “智能解读”模块上下间距与信息分组，避免内容粘连。
- 新增 `assets/tabbar/*.png` 并更新 `app.json`，为底部 Tab 补充图标与选中态视觉区分。
## 2026-04-22 WHO 生长曲线接入
- 下载 WHO 官方 `0-5 岁 Length/Height-for-age` 男女 percentiles expanded tables，并落库到 `assets/who/` 作为原始数据来源。
- 生成项目内可直接使用的 `utils/who-lhfa-data.json`，保留 `day / L / M / S / P3 / P50 / P97` 字段，支持本地插值计算。
- 新增 `utils/growth-standard.js`，实现年龄天数换算、WHO 标准插值、LMS z-score 计算、percentile 估算，以及曲线绘图数据构建。
- 重构 `components/growth-chart`，改为按 WHO `3% / 50% / 97%` 参考线和宝宝实测点绘制趋势图，并增加“最近”垂直标记。
- 调整 `pages/growth` 与 `utils/store.js`，实现 24 月以下显示“身长”、24 月及以上显示“身高”，并将智能解读改为基于 WHO 分位结果生成。
